// calculate area of square (side*side)
#include<stdio.h>
int main(){
    int side,sum=0;
    printf("enter length one side of the square :");
    scanf("%d",&side);
    //sum=side*side;
   // printf("area of the square  :%d",sum);
           //OR
    printf("area of the square  :%d",side*side);  
    return 0;

}
